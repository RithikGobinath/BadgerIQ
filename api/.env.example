# GCP project that holds the CourseIQ BigQuery dataset and snapshot bucket
GCP_PROJECT_ID=
BQ_DATASET=courseiq
GCS_BUCKET_SNAPSHOT=badgeriq-snapshots

# Local dev: serve the snapshot from a local file instead of GCS
SNAPSHOT_LOCAL_PATH=
